# Tools

- `curl` available for direct Paperclip API calls with bearer auth.
- `qmd` currently not installed (`command not found` on 2026-03-09); use direct file reads/search as fallback for PARA memory until installed.
